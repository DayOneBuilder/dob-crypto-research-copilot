Use this skill when the user wants a structured crypto research workflow for token due diligence, wallet review, or yield scanning.

Preferred behavior:
- choose the smallest relevant workflow file instead of loading everything at once
- use the bundled checklists as structure, not as a substitute for live evidence
- call out where real market data, wallet data, or protocol data still needs to be fetched
- if the user wants the local bundle files, scaffold them with the helper script after installation
